{% extends "APIBundle::layout.html.twig" %}

{% block title "starmade - Shops" %}

{% block content_header '' %}

{% block content %}
    <h1 class="title">Shop #{{ id }}</h1>

    <p>{{ shop }}</p>
    <p>
        <a href="{{ path('get_shops') }}">{{ 'shops.list.link'|trans({}, 'APIDemoBundle') }}</a> <br>
    </p>
{% endblock %}

{% set code = code(_self) %}
