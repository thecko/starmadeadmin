{% extends "APIBundle::layout.html.twig" %}

{% block title "starmade - Planets" %}

{% block content_header '' %}

{% block content %}
    <h1 class="title">Planet #{{ id }}</h1>

    <p>{{ planet }}</p>
    <p>
        <a href="{{ path('get_planets') }}">{{ 'planets.list.link'|trans({}, 'APIDemoBundle') }}</a> <br>
    </p>
{% endblock %}

{% set code = code(_self) %}
